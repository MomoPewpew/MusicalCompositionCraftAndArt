services:
  web:
    build: .
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - web_node_modules:/app/web/node_modules
      - web_next_cache:/app/web/.next

volumes:
  web_node_modules:
  web_next_cache:
